export enum RoutesEnum {
    Film = '/',
    Filters = '/filters',
    Filter = ''
}

export enum RoutesFilmType {
    all =  '/all',
    films = '/films',
    tvseries = '/tvseries',
    miniseries = '/miniseries',
    shows = '/shows'
}